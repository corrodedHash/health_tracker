//! `health-bot` — Matrix listener binary.
//!
//! Checkpoint stub: porting matrix-sdk skeleton + GPX/haversine logic
//! from `matrix-running` happens next session; see `MIGRATION.md`.

fn main() {
    eprintln!("health-bot: stub binary, see MIGRATION.md");
}
