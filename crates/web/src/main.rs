//! `health-web` — axum server binary.
//!
//! Checkpoint stub: routes + OIDC integration + static serving arrive next
//! session; see `MIGRATION.md`.

fn main() {
    eprintln!("health-web: stub binary, see MIGRATION.md");
}
