target/

# Frontend
frontend/node_modules/
frontend/dist/

# Bot session cache (contains matrix-sdk MatrixSession)
session.toml
*.session.toml

# Local config overrides (default.toml is committed; local-only layers are not)
config/local.toml
config/*.local.toml

# SQLx offline cache — committed so every build is type-safe without a live DB
# .sqlx/

# Editor / OS cruft
.DS_Store
*.swp
*.swo
.idea/
.vscode/
*.code-workspace